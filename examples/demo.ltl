// examples/demo.ltl — runnable demo for ltl_png_read

import std::io
use ltl_png_read as lib

fn main() {
    io::println("ltl_png_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}